apiVersion: apps/v1
kind: Deployment
metadata:
  name: app1-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: app1
  template:
    metadata:
      labels:
        app: app1
    spec:
      containers:
        - name: app1-container
          image: nginx:latest
          ports:
            - containerPort: 80
          volumeMounts:
          - name: app1-configmap
            mountPath: /usr/share/nginx/html/index.html
            subPath: index.html
      volumes:
      - name: app1-configmap
        configMap:
          name: app1-configmap
          items:
          - key: index.html
            path: index.html
